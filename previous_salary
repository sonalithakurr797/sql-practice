select employee_id, effective_date, salary,previous_salary,salary - previous_salary as salary_change
from 
(select employee_id, effective_date, salary,
lag(salary) over(partition by employee_id) as previous_salary
from employee_salary_history)

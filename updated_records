select id,first_name,last_name,salary,department_id from
(select *,
row_number() over(partition by id order by salary desc,department_id desc) as rn
from ms_employee_salary)s
where rn = 1
order by id asc
